## Da modificare

<!-- google-mcp -->


1. ✅ Nessun contatto visibile. L'unico accesso a contatti è il FAB: 4 icone senza etichetta in basso al centro. Un recruiter con 30 secondi non lo esplora. Serve un punto di contatto esplicito: una mini-bar (mail · LinkedIn · GitHub) nel fold o un footer di chiusura — vedi punto 5.
    — **Fatto (2026-07-10):** footer di chiusura (`.cv-footer__credit` in `[mode].astro` + `en/cv.astro`) ora mostra Nome e cognome · Telefono (link `tel:`) · LinkedIn · Email per esteso, derivati da `cvData.personal`/`cvData.social`. Nuovo campo `personal.phone` in cv.ts, mirrorato in cv.en.ts.

2. **Deciso (2026-07-10): NON implementare.** "Scarica CV PDF" esplicitamente rifiutato dall'utente — il footer resta solo con i contatti (punto 1).

3. ✅ La og:image è il favicon SVG (Layout.astro:21). LinkedIn e WhatsApp non renderizzano SVG come preview — quando condividi il sito in candidatura, il link appare senza immagine. Serve una PNG/JPG 1200×630 reale (nome + knolling + tagline): è probabilmente il fix con il miglior rapporto impatto/sforzo di tutta la lista, perché il primo contatto col tuo CV spesso avviene proprio dentro un feed o una chat.

4. ✅ La tagline parla ai clienti, non ai recruiter. "Accelero la crescita delle PMI con Tecnologia, Design e Metodo" posiziona un consulente fractional. Chi valuta un'assunzione si chiede "ma è assumibile?". Non la stravolgerei (il posizionamento PMI resta valido), ma aggiungerei una riga secondaria concreta e neutra, es. "6+ anni frontend · UX certificato IBM · AI workflow" — i tre pilastri tradotti in credenziali verificabili.

5. ✅ La pagina finisce nel nulla. Dopo la sezione AI & Digital Specialist non c'è footer: niente mail, social, P.IVA, download. Chi ha scrollato tutto è il visitatore più caldo e non trova il commit finale. Un footer leggero (contatti + PDF + EN/IT) risolve anche i punti 1 e 2 in un colpo solo.
    — **Fatto (2026-07-10), scope ridotto su decisione utente:** footer con contatti (vedi punto 1). PDF download e switch EN/IT nel footer esplicitamente esclusi (punto 2) — il lang-switch resta solo dov'era già (`.lang-switch` in home/nav).

Flusso e navigazione
6. ❌ Zero segnali di scroll su desktop. Il fold è 100svh con overflow: hidden — sembra una pagina conclusa. Il commento nel codice dice "Scroll cue — scompare al primo scroll" ma l'elemento non esiste più. Le frecce ↓ compaiono solo dopo aver selezionato una card. Due opzioni: reintrodurre un piccolo chevron animato sotto le card, oppure (più elegante) lasciar intravedere i primi pixel della prima profile-section sotto il fold — l'affordance spaziale non ha bisogno di spiegazioni.

7. Le preview non giustificano ancora il passaggio intermedio. Col nuovo flusso card → scroll → preview → pagina, ogni profile-section deve aggiungere qualcosa rispetto alla card, ma oggi eyebrow e titolo duplicano label e desc della card, più 3 righe e un marquee. Il design system del progetto prevede già la risposta: social proof visiva — un badge impactScore (-87% boilerplate, +3x velocità) in font mono, un progetto reale con thumbnail, o una quote dai feedbacks di cv.ts. Così la home diventa vetrina/portfolio vera, non un menu lungo.

8. Sezioni troppo lunghe e vuote, soprattutto mobile (~4 sezioni × 1.5 viewport con un paragrafo ciascuna — la pagina mobile è ~3.4 viewport di cui metà verde vuoto). Compattare il padding verticale e densificare col punto 7. Il parallax a delta 300px su desktop inoltre sposta le immagini knolling molto lontano dalla posizione percepita della sezione.

Contenuto
9. ✅ Il marquee management viola il vocabolario del progetto: contiene "Stakeholder Management", "KPI", "OKR", "Team Leadership", "Product Owner" — tutti termini nella blacklist della tua stessa regola ferrea. Sostituirli con il lessico GO: Sprint 1–2 settimane, impactScore, Backlog orientato al business, Lean, Facilitatore, Referente Unico, Rilasci misurabili.

10. ✅ Meta description della home debole: "Mi piace risolvere i problemi digitali e di comunicazione" — simpatica ma senza keyword né ruoli. La default del Layout ("Senior Frontend Developer, UX Designer, Creative Technologist") è già migliore: usarla o scriverne una con i ruoli.

Micro-UI
✅ Il cursor custom appare fisso a 0,0 (angolo alto-sinistra) finché il mouse non si muove — visibile in ogni screenshot. Nasconderlo fino al primo mousemove.
✅ (superato) Il FAB, se resta, merita label/tooltip al primo caricamento: quattro glifi senza testo sono un lucchetto sulle tue informazioni di contatto.

---

## Pagina [mode] — analisi 2026-07-07 (screenshot Playwright /tech desktop 1440px + mobile 393px)

### Posizionamento & obiettivo (T-shaped → Creative Developer / UX-UI)

11. ✅ L'hero-title è identico su tutte e 4 le mode page: "Consulente per l'Innovazione Digitale & Partner Tecnico per PMI" (`cvData.personal.title`). Chi arriva su /tech o /creative cercando un frontend/creative developer legge "consulente PMI" — il timore che il posizionamento PMI chiuda porte è fondato proprio qui. Fix: hero-title (e summary) mode-aware — le `modeDescriptions` esistono GIÀ nel frontmatter di [mode].astro ma alimentano solo la meta description. /tech → "Senior Frontend Developer — Angular, Lit, TypeScript, AI workflow" · /creative → "Creative Developer & UX/UI Designer — cert. IBM". Il posizionamento PMI resta su home e /management.
    — **Fatto (2026-07-10):** `heroTitles`/`heroSummaries` mode-aware aggiunti in `[mode].astro` (stesso pattern di `modeDescriptions`), tono formale, ogni summary racconta un'esperienza reale già presente in cv.ts (ALTEN/Aruba/Intesa San Paolo per tech, cert. IBM + Aruba per creative, B-Teatro + Music Agency per human, Scrum/Kanban su Intesa San Paolo/Aruba/Music Agency per management). **Non** usato "Senior Frontend Developer" come da esempio originale del punto — coerente con la memoria "posizionamento-ux-ui-first" (2026-07-09): Design/UX-UI sempre davanti, Tecnologia come strumento. EN (`en/cv.astro`, pagina statica single-mode) ha ricevuto solo la versione "tech" tradotta, essendo l'unica persona di default lì — nessuno switch di testo per mode lato client, solo le card cambiano stato.

12. ✅ Valutare rinomina del mode creative: "Web & UX Designer" → "Creative Developer". È il ruolo di mercato che unisce i due lati del profilo T (dev + design + animazione GSAP), è una keyword riconosciuta (mondo Awwwards) ed è esattamente ciò che questo sito dimostra di saper fare. "Web & UX Designer" sottovende la parte dev.
    — **Deciso (2026-07-08): si MANTIENE "Web & UX Designer".** L'obiettivo si è affinato in "web designer, T-shaped nella comunicazione, team/sprint" — la label attuale matcha 1:1 le keyword dei ruoli cercati (ATS + ricerche recruiter); il lato dev resta dimostrato dal mode tech. Nessuna modifica al codice necessaria.

13. ✅ La narrativa T-shaped è sepolta nella sezione Mindset (~60% della pagina). Per l'obiettivo dichiarato va portata nel fold: una riga visiva nel hero (profondità = frontend 6+ anni · ampiezza = design, metodo, AI, palco) o riusare il force graph come prova esplicita della T.
    — **Fatto (2026-07-10):** nuovo blocco `.hero-tshape` nel fold, subito sotto il summary, in `[mode].astro` + `en/cv.astro`: "Profondità → Frontend · 6+ anni" / glifo "⊤" / "Ampiezza → Design · Metodo · AI · Palco" (contenuto letterale suggerito dal punto). Incluso nella sequenza GSAP d'ingresso e nella lista CSS di stato iniziale nascosto (`cv-init.ts` + `cv-page.css`), stesso trattamento delle altre righe hero. Il contenuto della sezione Mindset in fondo pagina resta invariato — questa è una sintesi anticipata, non una sostituzione.

14. ✅ Progetti è la sezione più debole per un target creative developer: solo card testuali, nessuno screenshot, nessun link live visibile. Il portfolio visivo È la prova d'assunzione in ambito creative/UX. Aggiungere: thumbnail, link live/GitHub evidenti, e il digital-cv stesso come case study con impactScore.
    — **Piano deciso (2026-07-08), implementato (2026-07-09) — vedi punto 32.**

### Decisioni di design (2026-07-08) — analisi mode section + portfolio

- **Mode section: si mantiene il sistema a sussurri** (opzione "così com'è + fix"). Nascondere le parti non-mode contraddirebbe la tesi del sito (knolling = inventario a trasparenza radicale) e sottovenderebbe il profilo T proprio mentre lo si dichiara: chi valuta un T-shaped deve vedere l'ampiezza anche quando guarda la profondità. Il difetto era l'esecuzione, già sistemata (passive 0.55 + attive in cima, punto 17). Niente filtro hide dalla dropdown.
- **Case study: pagine dedicate `/work/[slug]`, non modali.** Motivo: URL condivisibili (campo portfolio LinkedIn, messaggi ai recruiter, candidature), SEO, stampa; le modali non hanno URL e su mobile sono fragili. Pattern atteso dai recruiter (Jakob).
- **Primi due case study: Digital CV (questo sito) + Music Agency.** Insieme coprono i due lati della T: design/dev (design system, GSAP, AI workflow) + comunicazione (+100% follower organici, strategia, eventi). Aruba DS rimandato (probabili vincoli NDA sui visual → servono diagrammi astratti).
- **Label mode creative: resta "Web & UX Designer"** (punto 12 chiuso).

32. ✅ Sistema case study — implementazione: estendere `projects` in cv.ts con campi opzionali case-study (slug, problem, role e contesto team/sprint, process[] 3-4 step, outcomes[] con impactScore, images[], testimonial dai feedbacks) — MIRROR OBBLIGATORIO in cv.en.ts; route `/work/[slug].astro` con template: hero visivo → ruolo+team → problema → processo → risultato misurabile → CTA contatto in fondo (chi finisce un case study è il visitatore più caldo). La griglia Progetti diventa l'indice (chiude anche il punto 14). Partire con digital-cv e music-agency.
    — **Fatto (2026-07-09):** `Project` esteso in cv.ts/cv.en.ts con slug/primaryMode/role/problem/process[]/outcomes[] (opzionali, non rompono i progetti esistenti). Testimonial e images[] NON aggiunti: nessuna quote reale disponibile nei feedbacks (solo keywords, zero campo `quote` popolato) e nessuno screenshot reale del progetto — meglio ometterli che inventare contenuto falso; facili da aggiungere dopo. Contenuto dei due case study interamente sourced da dati già esistenti e approvati in cv.ts (esperienza Music Agency, esperienza/progetto Digital CV — incluso "consegnato in 5 giorni contro una stima di un mese", ">80% copertura test", "+100% follower organici"), zero numeri inventati. Route `/work/[slug].astro` + mirror `/en/work/[slug].astro` via `getStaticPaths()`, nuovo `work-page.css`. `ProjectCard.astro` mostra link "Case study" quando il progetto ha uno slug. Bonus: bottone "Portfolio" nell'hero-socials → ancora `#projects` in pagina. Build Astro completa verificata (13 pagine, incluse le 4 nuove case study) + 55 test data-integrity/parity IT-EN + 42 test cv-site, tutti verdi.

33. (Micro-fix opzionale, mode section) Chip conteggio in font mono sulle section title filtrate ("12 attive · 8 in ombra") — rende il sistema sussurri leggibile come sistema intenzionale, non contenuto rotto.

34. ✅ Case study `/work/digital-cv` — aggiungere la **sezione Design System dedicata** alla pagina: lo step 04 del processo lo cita (token ottanio + 4 accent, Lexend/JetBrains Mono, square/glow, regole animazione) ma la pagina non lo mostra ancora visivamente.
    — **Fatto (2026-07-10):** nuovo componente `WorkDesignSystem.astro` (bilingue via prop) renderizzato su /work/digital-cv IT+EN tra Decisioni chiave e Risultato misurabile, con anchor `#design-system` e CTA "Esplora il design system ↓" nel hero. Contenuto: palette base + 4 accent con muted WCAG e ratio, specimen tipografico Lexend/JetBrains Mono con scala, demo live square/glow (bordo 1→4px + glow), regole motion coi token reali, superfici & segni (radius 1rem, touch-icon mask, chevron CSS, knolling WebP), demo mode system (pill attiva + sussurri). Tutti valori reali da global.css/design-system skill. Nella stessa sessione: **`--radius-card: 1rem`** introdotto in global.css e applicato a tutte le card/sezioni/sottosezioni (exp-cluster, exp/ai/project/soft card, skill-square, skills-stage, growth/social-impact card, mode-card home, work-*, pannello feedback FAB) — chip, pill, tooltip e controlli nav restano com'erano.
    — **Resta da fare** (quando ci sono i materiali): screenshot reali del sito nel case study, link playlist + estratti testi serata per /work/music-agency, campo `images[]`/testimonial (punto 32).

### Contenuto & convenzioni (Legge di Jakob)

15. ✅ Ordine esperienze nel cluster "Sviluppo Software & AI": le prime due card sono "Progetto Interno — Gestionale PMI" e "Digital CV — Progetto Open Source"; ALTEN Italia (il datore attuale, Senior Frontend Developer) è terza. I recruiter leggono in reverse-chronological e cercano "dove lavora ora": ALTEN deve essere la prima card; i progetti personali dopo, etichettati chiaramente come case study.
    — **Fatto (2026-07-08):** riordinati gli `indices` del cluster tech in [mode].astro e en/cv.astro: `[0,1,2,...]` → `[2,0,1,...]`. ALTEN featured per primo, poi i due progetti personali (già auto-etichettati come caso studio nel nome), poi i ruoli passati in reverse-chron nel drawer. `EXP_MODE_TAGS` non toccato (mappa per indice originale dell'array experience, non per posizione nel cluster). Verificato con screenshot Playwright.

16. ✅ Il force graph come vista skills di DEFAULT viola le convenzioni di scansione (Jakob): impressiona ma non si scanna. La vista CARD esiste già — valutare: default CARD (scannabile in 5s), graph come opt-in "wow". Su mobile a maggior ragione (drag vs scroll).
    — **Fatto (2026-07-08):** default CARD su tutti i viewport (prima era graph su desktop, cards su mobile). Toccati: markup SSR in [mode].astro + en/cv.astro (`data-skills-view="cards"` sulla section, bottone CARD primo e attivo, subtitle non più graph-centrica: "livello & glow proporzionali — apri il Grafico per esplorare le connessioni"), cv-init.ts (`applySkillsView("cards")` incondizionato). Bonus perf: il lazy-load di SkillForceGraph (D3, ~130KB gzip) non è più su IntersectionObserver della sezione ma on-demand al primo click su "Grafico" — chi non apre il grafo non scarica mai D3. Verificato: vista card di default + switch al grafo funzionante (custom element registrato dopo il click).

17. ✅ Soft Skills in mode tech = muro di righe quasi invisibili (quasi tutte passive/sussurro). A colpo d'occhio sembra una sezione vuota/rotta. Alzare l'opacity delle passive o riordinare le attive in cima.
    — **Fatto (2026-07-08):** entrambe le cose, in SoftItem.astro (vale per Soft Skills E Mindset): (a) `--card-opacity-passive: 0.55` sull'item — override via custom property, vince sul valore per-mode di global.css (0.2) senza lotte di specificità perché la regola globale legge già `var(--card-opacity-passive)`; "sussurro" leggibile ma chiaramente secondario; (b) `order: 1` sulle passive — .soft-list/.method-list sono flex column, le attive salgono in cima. Verificato con screenshot: attive (Problem solving laterale, Pensiero T-shaped, ecc.) in cima a piena opacità, passive sotto e leggibili.

### Struttura & a11y

18. ✅ soft-section, growth-section, projects-section, edu-section e footer sono figli diretti di <body>, FUORI da <main id="main-content"> (verificato via DOM probe). Landmark semantico rotto: screen reader e skip-link coprono solo hero→AI section. Spostarli dentro <main>.
    — **Fatto (2026-07-07):** causa radice trovata durante il test dell'utente — in `[mode].astro` il tag `</Layout>` chiudeva subito dopo `.soft-section` (riga 733), quindi growth/projects/edu/footer erano fuori anche dal componente `<Layout>`, non solo da `<main>`. Il browser li "riattaccava" al `<body>` per tolleranza del parser HTML, ma perdevano tutto lo styling di `.cv-main` (`max-width`, `padding`, e soprattutto `gap: 4rem` tra sezioni) — la causa esatta del punto 23 sotto. `en/cv.astro` non aveva questo bug (era già strutturato correttamente): le due lingue erano divergenti anche a livello di markup, non solo di testo.

### LinkedIn (allegato Profile.csv)

19. Il profilo LinkedIn non ha il sito nei Websites e l'headline è "Frontend Developer presso ALTEN Italia" — non allineata né al sito né all'obiettivo. Azioni fuori-repo ma critiche: aggiungere l'URL del digital CV su LinkedIn; headline coerente col target, es. "Creative Developer · Frontend (Angular/Lit) · UX/UI cert. IBM".

### Lente recruiter & mercato 2026 (2026-07-07)

I ruoli "puro frontend implementatore" e "full-stack generico" sono in erosione (l'AI copre il boilerplate). In crescita: Creative Developer / Design Engineer (dev + occhio per interazione/animazione — il mondo Awwwards) e AI Workflow Designer/Orchestrator (dirige agenti AI invece di scrivere ogni riga a mano). Il posizionamento T-shaped di Giulio è la scommessa giusta per questi ruoli emergenti — ma va dimostrato con la prova (portfolio, case study), non dichiarato con l'aggettivo. Collega ai punti 11-14 sopra.

20. ✅ Awwwards ambition — scroll parallax/transizione tra section anche su [mode].astro. Oggi [mode] usa solo fade-reveal (`ScrollTrigger.batch` in cv-init.ts:663), nessun parallax tra sezioni — la home invece ce l'ha già su `.profile-knoll-img` con `scrub:true`. Estendere lo stesso linguaggio (parallax leggero su `bg-knoll` + variazione di luce/tinta ambientale) tra Skills → Esperienze → AI Workflow, mai scroll-jacking/pin che blocca lo scroll naturale (un recruiter con fretta che si sente "prigioniero" chiude il tab).
    — **Fatto (2026-07-07):** correzione all'analisi: il parallax sui `bg-knoll` ESISTEVA già in cv-init.ts (delta 500/300, scrub, reduced-motion ok) — quello che mancava era la transizione percettiva tra sezioni. Aggiunto: (a) luce ambientale per sezione — `::before` con radial-gradient accent al 5% su skills/exp/ai/soft/projects/edu-section (cv-page.css) e su `.profile-section` in home (index-page.css, finalmente usando il token `--ps-glow` che era definito ma mai referenziato), con `--ambient` 0→1 scrubbata da GSAP all'ingresso della sezione (cv-init.ts + index-init.ts); (b) profondità tra sezioni — `.section-title` scorre ±24px più lento del contenuto (scrub). Tutto dentro i blocchi già protetti da prefers-reduced-motion; niente scroll-jacking/pin.

21. ✅ Esperienze troppo lunghe e "pompose"/AI-sounding. Confermato leggendo cv.ts: 22 esperienze, molte con 5-6 highlight, descrizioni strutturate "Fase 1/2/3/4", linguaggio da press release ("banco di prova sistematico", "l'AI come moltiplicatore"), percentuali ovunque. Da recruiter: questo pattern si riconosce a colpo d'occhio come AI-generato e fa perdere autenticità anche quando il lavoro dietro è vero e forte (Aruba Design System 30+ persone, Intesa San Paolo). Fix: passaggio di editing su tutte le 22 — description più corta e umana, highlights tagliati a 2-3 max (oggi ExpCard.astro ne mostra già solo 3 in featured ma la sorgente ne ha fino a 6, e lo stato "active" espande a 10 righe via line-clamp), via la struttura "Fase 1-2-3-4".
    — **Fatto (2026-07-07):** riscritte tutte le 14 description/highlights "gonfie" in `cv.ts` (IT) e specchiate in `cv.en.ts` (EN) — struttura "Fase 1-2-3-4" rimossa, linguaggio da press release ridotto, highlights tagliati a max 3 ovunque. Le 8 esperienze già concise (Bestar Hotel, UCI Cinemas originariamente corta, Starbucks, Sogni Animazione, Gruppo Mondadori, B-Teatro, Freelance Fotografo, ecc.) lasciate invariate. Type-check e 84 test Vitest passano. Rinforzata anche la regola in `mcp-architecture/SKILL.md` (Data Layer): il mirroring IT→EN vale per i contenuti, non solo per la struttura dei tipi.

22. ✅ "Read more" incrementale (3 alla volta) al posto di "GO deeper" (reveal-all). Oggi il bottone in [mode].astro:648-663 rivela TUTTO l'"extra" del cluster in un colpo — con FEATURED_N=3 e cluster da 4-6 item il problema non si vede ancora, ma il pattern non scala se si aggiungono esperienze. "Leggi altro" a lotti di 3 è una convenzione più riconoscibile (Legge di Jakob — pattern di LinkedIn/Medium/ogni feed) e permette un reveal progressivo invece di un drawer che si apre tutto insieme.
    — **Fatto (2026-07-07):** sostituito il toggle GO deeper (drawer height-animato open/close) con reveal incrementale: le card extra partono `[hidden]`, ogni click rivela le successive 3 con stagger GSAP, label con conteggio ("Leggi altre N" IT / "Read N more" EN via `document.documentElement.lang`), bottone scompare quando non resta nulla — nessuno stato "chiudi". Toccati: cv-init.ts (logica), cv-page.css (via height:0/overflow del drawer e regole aria-expanded morte; aggiunto `.exp-card[hidden]{display:none}` perché il display:flex della card vincerebbe sul default UA), [mode].astro + en/cv.astro (markup: via aria-expanded/aria-hidden/data-open-label). Aggiunta eccezione alla regola CTA in identity/SKILL.md: read-more con conteggio ok per il disclosure progressivo nelle liste, "GO" resta per la navigazione.

### Bug trovati testando 20/22 in browser (2026-07-07)

23. ✅ Bottone "Leggi altre N" non scompariva a fine reveal, restava a schermo. Causa: stesso conflitto di specificità già noto per `.exp-card[hidden]` — `.exp-deeper-btn { display: inline-flex }` (autore) vince su `[hidden] { display: none }` (UA) a parità di specificità. **Fix:** aggiunta `.exp-deeper-btn[hidden] { display: none }` in cv-page.css.

24. ✅ Spacing/padding mancante da Soft Skills in giù. Causa radice: bug strutturale in `[mode].astro` — vedi nota aggiornata al punto 18. Le sezioni erano fuori da `.cv-main` (che fornisce `gap: 4rem` + `max-width` + padding) perché fuori persino da `<Layout>`. **Fix:** spostato `</main>` e `</Layout>` a wrappare correttamente soft/growth/projects/edu-section, footer resta sibling di main (semantica corretta) ma dentro Layout.

25. ✅ Sezione "AI-Enhanced Workflow" appariva sbiadita/"disabilitata" su mode senza tag corrispondente (es. management — nessun item di `aiWorkflow` ha tag "management"). Causa: `AiCard.astro` metteva `class="ai-card cv-card" data-tags={tags}`, quindi `applyCardStates()` la trattava come le altre card mode-filtrate, contraddicendo la regola documentata "AI Workflow sempre visibile indipendentemente dal mode" (mcp-architecture/SKILL.md). **Fix:** rimossi `cv-card` e `data-tags` da AiCard — la sezione non entra più nel sistema active/passive, resta sempre a piena opacità.
    — **Nota non implementata:** la richiesta più ampia di "stile accordion per contenuti disabilitati/passivi altrove" (skill/card passive nascoste dietro un reveal, non solo mostrate a bassa opacity) **contraddice una regola non negoziabile del design system** ("No display:none per le card passive — sono sussurri, non silenzi", knolling-cv/SKILL.md). Da discutere esplicitamente con l'utente prima di implementare, perché è un cambio di filosofia narrativa, non un bug fix.

26. ✅ Navbar desktop — le voci mode non selezionate erano testo grigio piatto, nessun accenno al colore del proprio mode. **Fix:** aggiunte custom property `--btn-accent` per mode su `.mode-btn[data-nav-mode=...]` (stessi valori fissi tech/creative/human/management del design system) e, solo su desktop (`min-width:40.0625rem`), bordo+testo delle voci non attive tinti nel proprio accent a bassa intensità (`color-mix` 30-55%), hover leggermente più marcato. La voce attiva non cambia (resta piena intensità via `[aria-pressed="true"]`).

27. ✅ Al cambio mode lo "stamp" fullscreen (l'effetto editorial a schermo intero con parallax/scan che accompagna il cambio) mostrava la chiave interna grezza in maiuscolo ("TECH", "HUMAN", "CREATIVE", "MANAGEMENT") invece della label leggibile — `stamp.textContent = mode.toUpperCase()` in cv-init.ts:349. **Fix:** usa `MODE_LABELS[mode]` (la stessa mappa già usata per il trigger della dropdown) invece della chiave grezza.

28. ✅ Le voci della dropdown mode (mobile, ≤640px) erano troppo lunghe per il contenitore e troncavano con ellissi ("AI & DIGITAL S…"). **Fix:** accorciate le label — "Software Developer" → "Software Dev", "Web & UX Designer" → "Web & UX", "AI & Digital Specialist" → "AI & Digital"; "Project Manager" invariato su richiesta. Toccati 4 punti che dovevano restare in sync: trigger SSR e lista `<li>` in [mode].astro e en/cv.astro, più `MODE_LABELS` in cv-init.ts (che ora alimenta anche lo stamp del punto 27, coerente in un colpo solo).

29. ✅ Click sugli oggetti knolling (home) doveva solo "attivare" la mode-card corrispondente (gamification/scoperta), senza far scattare anche lo scrollToProfile che oggi parte da un click sulla card stessa. Prima gli oggetti inoltravano un `targetCard.click()` sintetico, che rientrava nell'handler completo della card (select + scroll). **Fix:** gli oggetti knolling (`.knoll-item` desktop e `.knoll-wrap` mobile, incluso il caso torcia con hit-area ridotta) ora chiamano direttamente `selectMode(targetCard, primaryMode)` invece di simulare il click — attivano la card e il knolling reagisce (is-hero/is-dim), ma non scrollano. Lo scroll resta un'azione esplicita solo sul tap della card.

30. ✅ Aprendo un cluster esperienze in [mode] ("dropdown"), lo scroll finiva verso la FINE del contenuto appena aperto invece che all'inizio. Causa: `.exp-cluster__body` transiziona `max-height: 0 → 9000px` (cv-page.css:1143-1153, hack per permettere contenuto di altezza arbitraria) e il codice spostava il focus sul body appena apertosi (`body.focus({preventScroll:true})`, cv-init.ts) — durante la transizione max-height alcuni browser "inseguono" l'elemento a fuoco e finiscono per allineare la fine del box invece dell'inizio, nonostante `preventScroll`. **Fix:** aggiunto uno scroll esplicito verso l'HEADER del cluster (che non si sposta, a differenza del body) tramite `scrollToPositionThen` — helper già esistente in cv-init.ts, usato altrove per lo scan del mode-switch — eseguito 600ms dopo l'apertura (dopo che la transizione da 0.55s e l'eventuale chiusura degli altri cluster su mobile si sono assestate). Il focus per la tastiera resta invariato, solo lo scroll pagina è ora controllato esplicitamente.

31. ✅ Ripristinate le 4 mode-card su mobile (≤639px) come griglia 2×2 "knolling style" al posto della colonna di righe orizzontali. Interventi in index-page.css: (a) rimosso l'override mobile-only che forzava flex-column — ora cascata la stessa griglia 2×2 del breakpoint touch, gap costante 0.4rem in entrambe le direzioni (regola knolling: lo spazio tra gli oggetti è parte del design); (b) aggiunto cartellino quadrato accent (`.mode-card__label::before`, solo touch) come etichetta d'inventario knolling — sostituisce il color-coding del GO button nascosto su touch, senza essere l'unico differenziatore (WCAG 1.4.1); (c) desc della card ora nel colore muted per-mode (`--card-desc-color`, valori WCAG-verificati della tabella design system). Bonus: `expandMobileGrid` (l'espansione GSAP della cella selezionata al 60%) era silenziosamente rotto dall'override flex — animava gridTemplate* su un flex container — e ora torna a funzionare. Verificato con screenshot Playwright a 393px (Pixel 5) e 320px: nessun overflow, label max 2 righe, fold intatto.

## Analisi codice 2026-07-12 (richiesta: proporre migliorie/fix e correggere)

Perimetro rivisto: pagine Astro (index/[mode]/en/work), scripts (memory-drawer, work-journey), componenti card, Layout, dati cv.ts/cv.en.ts. Backend src/ non toccato (84 test verdi, nessun sintomo).

35. ✅ **Bug navigazione EN → IT.** Le pagine work inglesi rimandavano alle pagine italiane: `en/work/index.astro` aveva "Back to CV" → `/tech`, `en/work/[slug].astro` aveva nav e CTA footer → `/${mode}` (route IT). Un recruiter anglofono che esplorava un case study finiva sul CV in italiano senza preavviso.
    — **Fatto (2026-07-12):** tutti e tre i link ora puntano a `/en/cv`.

36. ✅ **Bug dati: `SOFT_MODE_TAGS` disallineato (9 tag per 11 soft skill).** In `[mode].astro` e `en/cv.astro` la mappa per indice era stata scritta prima dell'aggiunta di "Ascolto attivo" e "Assertività": le due rubavano i tag destinati a "Sensibilità estetica" (creative) e "Pensiero T-shaped" (tutti i mode), che finivano nel fallback attivo-ovunque. Enfasi visiva sbagliata su 4 skill in tutti i mode.
    — **Fatto (2026-07-12):** mappa estesa a 11 voci con commento per riga (nome skill accanto al tag), mirror EN.

37. ✅ **Guard di build sulle mappe per indice.** `EXP_MODE_TAGS` e `SOFT_MODE_TAGS` mappano per posizione sugli array di cv.ts: aggiungere o riordinare una voce nei dati disallinea silenziosamente l'enfasi (è esattamente ciò che è successo al punto 36).
    — **Fatto (2026-07-12):** check nel frontmatter di `[mode].astro` e `en/cv.astro`: se le lunghezze non coincidono la build fallisce con un messaggio che dice quale mappa aggiornare.

38. ✅ **A11y: foto dei memory drawer invisibili alla tastiera.** Le `.memory-photo` aprivano la lightbox solo via click su `<img>`: nessun focus, nessun ruolo, nessuna attivazione da tastiera (WCAG 2.1.1).
    — **Fatto (2026-07-12):** in `memory-drawer.ts` le foto ricevono `tabindex=0`, `role="button"`, `aria-label` localizzata ("Espandi la foto"/"Expand photo"); Enter e Spazio delegano al click (vale anche per la chiusura della lightbox). Bonus: anche l'aria-label del bottone chiudi della lightbox ora è localizzata.

39. ✅ **Email footer hardcoded.** `[mode].astro` ed `en/cv.astro` avevano il `mailto:` letterale nel template mentre cv.ts espone già il contatto in `social`: due punti di verità per lo stesso dato.
    — **Fatto (2026-07-12):** footer derivato da `social` (piattaforma Email), come già facevano le pagine work.

### Segnalazioni aperte (decisioni da prendere, non fix)

40. **EN: i 4 CTA "GO to..." della home inglese puntano tutti a `/en/cv`** perché non esistono route EN per-mode (`/en/tech`, ecc.): la pagina EN è single-persona con filtro client-side. Decidere se creare `en/[mode].astro` (parità piena con l'IT) o dichiarare esplicitamente la scelta single-page.
41. **I marquee della home elencano strumenti non presenti in cv.ts** (PostgreSQL, MongoDB, Docker, Adobe XD, Sketch, Photoshop, Illustrator, After Effects, Webflow, Podcast...). Sono decorativi e `aria-hidden`, ma in un colloquio ogni voce è argomentabile ("Perché c'è Docker se non è nelle skill?"). Da riallineare al contenuto reale di cv.ts o da sfoltire.
42. **`astro check` su Windows segnala il conflitto di casing `gsap/Flip.d.ts` vs `flip.d.ts`** (difetto del pacchetto GSAP, non nostro) più i preesistenti errori `screen.orientation.lock`. Non bloccano build né runtime: da ripulire solo se si vuole un check pulito (es. `skipLibCheck` mirato o pin di versione GSAP).

## Richieste 2026-07-13

43. ✅ **Rimosso il termine "PMI" da tutto il progetto**, non solo dai testi del sito: anche dalle skill `.claude/` e dai mirror `.github/` (copilot-instructions.md, skills), da `AGENTS.md` e `CLAUDE.md`. Sostituito con "realtà" (con qualificatore, es. "piccole e grandi realtà") o "aziende"; EN "SMB/SME" → "businesses". Regola scritta in `identity/writing-style.md` perché resti applicata ai testi futuri.

44. ✅ **Rimossa la voce "AI Workflow" dal FloatingMenu (FAB).** Il FAB ora ha solo Contattami e Feedback. Rimossi insieme: lo scroll-to-`#ai-section` (`_handleAI`, ~80 righe con gestione Lenis/fallback/focus), le stringhe i18n `aiLabel`/`aiAriaLabel`, e la logica `isHome`/`href` che serviva solo a quella voce (su home il FAB non mostrava comunque il link AI). La sezione AI Workflow resta raggiungibile da dot-nav e scroll naturale su `/tech /creative /human /management`. Documentazione aggiornata (AGENTS.md, knolling-cv/SKILL.md IT+EN) per non citare più il link AI-section nel FAB.

### Nota di verifica

- Il "vuoto hero desktop" visto nei primi screenshot era un artefatto del dev server Vite (504 Outdated Optimize Dep durante re-optimize), NON un bug del sito: verificato con probe (opacity 1, posizioni corrette) e re-screenshot.
- La duplicazione di contenuto nello screenshot full-page mobile era un artefatto di cattura (Lenis syncTouch + fullPage), NON DOM duplicato: verificato (1 h1, 1 hero-section, 1 skills-section).
